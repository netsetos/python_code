def count_flips(bits):
    flip=0
    for i in bits:
        if flip%2==0:
            if i ==1:
                flip+=1
        else:
            if i ==0:
                flip+=1
    return flip


bits=[0,0,1,0,1]
print(count_flips(bits))
